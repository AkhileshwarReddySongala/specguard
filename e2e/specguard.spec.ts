import { expect, test } from "@playwright/test";
const repositoryContext = { owner: "acme", repo: "widget", number: 7, title: "Contract test", agentsMarkdown: "# Contract\n- Never add lodash as a dependency.", agentsPath: "AGENTS.md" };
const demoContract = { sourceRules: [{ id: "rule-2", requirementQuote: "Do not add lodash or any new runtime dependency.", specLine: 2, level: "MUST" }], checks: [{ id: "rule-2", requirementQuote: "Do not add lodash or any new runtime dependency.", specLine: 2, level: "MUST", mode: "dependency", target: "lodash", rationale: "test" }], unexpressibleRules: [], compiler: "deterministic-fallback", compilerDiagnostics: [] };
const demoResult = { snapshot: { owner: "specguard", repo: "demo", number: 1, title: "Add an unauthorized dependency", unifiedDiff: "@@ -1 +1,2 @@\n+import _ from 'lodash';", changedFiles: [{ path: "app/page.tsx", status: "modified", content: "import _ from 'lodash';" }] }, contract: demoContract, findings: [{ id: "rule-2-app", requirementQuote: demoContract.checks[0].requirementQuote, specLine: 2, filePath: "app/page.tsx", line: 1, diffHunk: "+import _ from 'lodash';", violationType: "Unauthorized dependency", action: "Remove lodash.", source: "deterministic", confidence: "high", preExisting: false }], verdict: "merge_blocked", complianceScore: 0, diagnostics: [], judgmentUnavailable: false, providerStatus: "deterministic-only" };
test("a demo PR renders an evidence-linked blocked verdict", async ({ page }) => { await page.route("**/api/compile", (route) => route.fulfill({ contentType: "text/event-stream", body: `data: ${JSON.stringify({ type: "final", contract: demoContract })}\n\n` })); await page.route("**/api/analyze", (route) => route.fulfill({ contentType: "application/json", body: JSON.stringify(demoResult) })); await page.goto("/"); await page.getByRole("button", { name: "COMPILE CONTRACT" }).click(); await expect(page.getByText(/Contract compiled/)).toBeVisible(); await page.getByRole("button", { name: "ANALYZE PULL REQUEST" }).click(); await expect(page.getByRole("heading", { name: "MERGE BLOCKED" })).toBeVisible(); await page.getByRole("option").first().click(); await expect(page.getByRole("heading", { name: /CONTRACT/ })).toBeVisible(); await expect(page.getByRole("heading", { name: /CHANGED CODE/ })).toBeVisible(); });
test("loads a discovered AGENTS contract before compilation", async ({ page }) => { await page.route("**/api/pr-context", (route) => route.fulfill({ contentType: "application/json", body: JSON.stringify(repositoryContext) })); await page.goto("/"); await page.getByLabel("PUBLIC GITHUB PR URL").fill("https://github.com/acme/widget/pull/7"); await page.getByRole("button", { name: "LOAD CONTRACT" }).click(); await expect(page.getByText("Loaded from AGENTS.md")).toBeVisible(); await expect(page.getByLabel("CONTRACT")).toHaveValue(repositoryContext.agentsMarkdown); });
test("requires an explicit contract choice when AGENTS is absent", async ({ page }) => { await page.route("**/api/pr-context", (route) => route.fulfill({ contentType: "application/json", body: JSON.stringify({ ...repositoryContext, agentsMarkdown: null, agentsPath: null }) })); await page.goto("/"); await page.getByLabel("PUBLIC GITHUB PR URL").fill("https://github.com/acme/widget/pull/7"); await page.getByRole("button", { name: "LOAD CONTRACT" }).click(); await expect(page.getByText("No AGENTS.md found")).toBeVisible(); await page.getByRole("button", { name: "USE ILLUSTRATIVE SAMPLE" }).click(); await expect(page.getByText(/Example contract/)).toBeVisible(); await expect(page.getByRole("button", { name: "COMPILE CONTRACT" })).toBeEnabled(); });